{
  "name": "html-to-android",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build needed'",
    "cap:init": "npx cap init"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
